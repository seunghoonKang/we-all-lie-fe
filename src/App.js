// import { socket } from "./socket";
import Router from "./shared/Router";
import Layout from "./elements/Layout";

function App() {
  return (
    <Layout>
      <Router />
    </Layout>
  );
}

export default App;
